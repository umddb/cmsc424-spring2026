[36:17.100 --> 36:24.100]  So the next topic for us was going to be parallel query execution, which I think is really important to cover,
[36:24.100 --> 36:31.100]  because today pretty much all databases outside of things like Postgres are somewhat parallel,
[36:31.100 --> 36:41.100]  like Apache Spark, Snowflake, Redshift, most of these modern data warehouses or big data systems, they all do parallel execution.
[36:42.100 --> 36:44.100]  So I think it's important to understand them better.
[36:45.100 --> 36:52.100]  But in order to understand the query processing parts of it, we have to discuss a little bit about parallel and distributed architectures.
[36:52.100 --> 36:56.100]  So that's where we'll start and then we'll move on to the operations.
[36:57.100 --> 36:59.100]  So before I go there, any questions on any of this?
[37:00.100 --> 37:06.100]  I'll try to release the homework in the next day or two. I've started working on it, but I'm not finished with it yet.
[37:06.100 --> 37:16.100]  So, parallel and distributed architectures.
[37:16.100 --> 37:21.100]  Earlier databases, earlier computing hardware was a single machine.
[37:21.100 --> 37:26.100]  You had a CPU, you had a memory, you had a disk, and you were kind of there.
[37:26.100 --> 37:31.100]  And you scaled it up. By scaling up, you mean that we get bigger and bigger processors.
[37:31.100 --> 37:39.100]  So that's what got us supercomputers, the early supercomputers like Cray and others from 80s are the mainframes, IBM mainframes.
[37:39.100 --> 37:46.100]  They were not, they had huge compute power, but they are not the same as the modern data centers.
[37:46.100 --> 37:59.100]  In data centers, we have lots of small machines, whereas the supercomputers was like a really custom-built, single high-performance machine, very high-performance machine.
[37:59.100 --> 38:09.100]  The ability to scale up is limited. It gets very expensive to keep building up a high-performance system.
[38:09.100 --> 38:15.100]  You do build applications where you cannot avoid it, and that's why HPC is such a big deal.
[38:15.100 --> 38:22.100]  But for many other applications, the cost of the scaling up doesn't really work anymore.
[38:23.100 --> 38:30.100]  You have to scale out. And by scaling out, we mean that we use lots of computers together.
[38:30.100 --> 38:37.100]  That's more cost-effective than trying to use a single machine that's really high-performance.
[38:37.100 --> 38:44.100]  And then part of the reason why we need sort of distributed architectures is we need to meet the users where they are.
[38:44.100 --> 38:55.100]  In the last 20 years with internet and all of that, we have users using the same system from many places, like social networks is a key example, but there are of course many others.
[38:55.100 --> 39:03.100]  Because of this, the parallel and distributed issues have become very common today. You kind of cannot avoid them if you are in the data space.
[39:03.100 --> 39:08.100]  Everything is distributed. Everything is parallel.
[39:08.100 --> 39:13.100]  We talked about some parallel architectures back when we talked about computing hardware.
[39:13.100 --> 39:26.100]  Some terms to know here. Shared memory means that you have a whole bunch of CPUs or processors, of course. Shared memory, shared the risk.
[39:26.100 --> 39:35.100]  So shared memory is also typically considered more of a scale-up architecture because you have limited in your ability to increase the computing power.
[39:35.100 --> 39:52.100]  Whereas the architectures that we see most commonly today in data centers are these, more of a shared nothing architecture, where you have each processor or each CPU has its own memory, has its own disk.
[39:52.100 --> 39:57.100]  And they are all connected over a very fast interconnect.
[39:57.100 --> 40:09.100]  And what we really see in practice is more of a mix because we have a data center with a whole bunch of machines, but each of the machines themselves has many cores inside it.
[40:09.100 --> 40:21.100]  So we can have a more complex structure. And you can call it hierarchical if you want. This word really gets used. Shared nothing is still the more common way to refer to that.
[40:21.100 --> 40:31.100]  So the question for us is how do we do a join on a shared nothing system? Shared memory is relatively easy. Shared nothing is complicated.
[40:31.100 --> 40:38.100]  Because in shared nothing, in order to do a data operation, I have to move the data around. And that's what we're going to talk about.
[40:38.100 --> 40:44.100]  Whatever we discussed today, the place where you will see it most easily would be Spark.
[40:45.100 --> 40:52.100]  I keep referring to Spark because the thing about Spark, Apache Spark, is it is so easy to use. You can just get it started and start using it.
[40:52.100 --> 41:02.100]  And you can actually see what it's doing. And all the operations we discussed today or next class will be essentially in the context of something like Apache Spark.
[41:02.100 --> 41:08.100]  Another figure for parallel architecture, a modern shared memory system.
[41:09.100 --> 41:18.100]  So one thing that you realize, if you ever go deep in this space, is that in the last 20 years, the hardware profile has changed drastically.
[41:18.100 --> 41:27.100]  And this is not even counting the GPU space. Today, of course, GPUs and the discussion around AI dominates. This is even sort of before that.
[41:28.100 --> 41:36.100]  But even a shared memory system today is very complicated. Because what you have is a bunch of CPUs that are working together.
[41:36.100 --> 41:44.100]  They each have their own memory. So this looks more like a shared nothing, except these CPUs actually have access to each other's memory.
[41:44.100 --> 41:52.100]  In many cases, it's a single address space. So you could write programs that are not even aware that they are touching each other's memory.
[41:53.100 --> 42:00.100]  If the CPUs only had access to their own memory, that would be more like a shared nothing, although the disk is still shared.
[42:00.100 --> 42:08.100]  But in typical architectures, even though a CPU is closely connected to a memory bank, other CPUs have access to it.
[42:08.100 --> 42:11.100]  That's why it gets very messy.
[42:12.100 --> 42:27.100]  The word storage area network also gets used here to refer to essentially lots of compute nodes connected to lots of storage across a network, through a network.
[42:27.100 --> 42:31.100]  So this is more of a shared disk setting.
[42:31.100 --> 42:47.100]  So keeping it simple, very simple, because this really does not match reality today, between shared disk, shared memory and shared nothing, you can kind of draw some comparisons.
[42:47.100 --> 42:55.100]  In shared memory, the communication between processors tends to be very fast because they are all on a very fast interconnect.
[42:55.100 --> 42:58.100]  They are actually on the same bus in most cases.
[42:58.100 --> 43:06.100]  Whereas shared nothing, because you are talking over the network, over TCP IP usually, this is slower.
[43:06.100 --> 43:15.100]  And of course, there is a lot of research on bypassing TCP IP altogether, if you know, if you understand what I'm saying here, because I'm not going to cover that.
[43:15.100 --> 43:24.100]  And to modern data centers often have direct connections that bypass the network stack in order to make this more efficient.
[43:25.100 --> 43:31.100]  Shared memory really doesn't scale because memory bus becomes the bottleneck.
[43:31.100 --> 43:35.100]  For shared disk, the disk interconnect becomes a bottleneck.
[43:35.100 --> 43:37.100]  Shared nothing is very scalable.
[43:37.100 --> 43:39.100]  It is very hard to program.
[43:39.100 --> 43:42.100]  The problem with shared nothing is the programming.
[43:42.100 --> 43:45.100]  It is not easy.
[43:45.100 --> 43:50.100]  And but you could have hundreds of thousands of machines in a data center.
[43:50.100 --> 43:52.100]  And that works just fine.
[43:52.100 --> 43:57.100]  You cannot have thousands of processors in a shared memory system.
[43:57.100 --> 44:03.100]  The cost of that, the hardware cost of that would be prohibitive.
[44:03.100 --> 44:07.100]  For shared memory, you have to worry about things like cache coherency.
[44:07.100 --> 44:17.100]  The memory might be shared, but the caches are typically not shared, which means you need to make sure that there is no inconsistency in the cache.
[44:17.100 --> 44:23.100]  For shared nothing, the overall data processing is difficult.
[44:23.100 --> 44:27.100]  And we will talk about query processing right now, and we'll talk about transactions later.
[44:33.100 --> 44:37.100]  And again, this is not super accurate because shared disk is a bit weird.
[44:37.100 --> 44:40.100]  It's not a very well-defined term.
[44:40.100 --> 44:49.100]  Shared memory gets used in high-performance settings where you really need performance.
[44:49.100 --> 45:01.100]  For instance, Amazon's main e-commerce site ran on a big shared memory system because that needed to be extremely fast.
[45:02.100 --> 45:08.100]  Even though Amazon pioneered cloud services in many ways with AWS and so on,
[45:08.100 --> 45:14.100]  the e-commerce itself was, when I had last checked some time ago,
[45:14.100 --> 45:20.100]  was a shared memory system, very high-performance, on top of an Oracle database.
[45:20.100 --> 45:24.100]  But this was a while ago. I'm sure it has changed since then.
[45:24.100 --> 45:30.100]  And that's a setting. The e-commerce setting is the one that Amazon really needs to be as fast as possible.
[45:30.100 --> 45:32.100]  And that's why they use shared memory there.
[45:34.100 --> 45:37.100]  Let me pause here for a second. Any questions on this?
[45:37.100 --> 45:40.100]  Again, think of this as general background on this topic,
[45:40.100 --> 45:46.100]  and feel free to ask any questions that you might have related to this.
[45:47.100 --> 45:50.100]  We have some time to discuss this.
[45:50.100 --> 46:00.100]  So, in the, I don't have a good figure ready here,
[46:00.100 --> 46:05.100]  but in this situation like this, each CPU has its own cache.
[46:05.100 --> 46:10.100]  What if the same item is present in both those caches?
[46:10.100 --> 46:16.100]  You need to make sure that if one of the CPUs updates it, the other CPU's cache reflects that update.
[46:16.100 --> 46:21.100]  That's not very easy to guarantee, because these CPUs are kind of running their own processes,
[46:21.100 --> 46:27.100]  and one of them may go ahead and update an item in its own cache without writing it to memory, so it's in its own cache.
[46:27.100 --> 46:32.100]  This CPU also has an item in its cache, but now that is invalid.
[46:32.100 --> 46:35.100]  And so you kind of need to figure out how to fix that.
[46:35.100 --> 46:37.100]  And it does not have an easy answer.
[46:37.100 --> 46:42.100]  Cache coherency is a tricky topic.
[46:42.100 --> 46:48.100]  And this is typically handled at the hardware level, to my knowledge, but I'm sure there are different solutions to it.
[46:54.100 --> 46:57.100]  Other questions on this?
[46:57.100 --> 47:06.100]  Again, I want to remind you that almost all modern systems run on some sort of a parallel architecture today.
[47:07.100 --> 47:15.100]  And a lot of research, a lot of industry work in databases or data systems in general,
[47:15.100 --> 47:20.100]  be it machine learning model training, use of machine learning models,
[47:20.100 --> 47:24.100]  all of that is typically running on some sort of parallel architectures.
[47:27.100 --> 47:34.100]  Some random terminology that we should be aware of.
[47:36.100 --> 47:43.100]  Some of this is just from the book, and I don't, honestly, one problem I have with this topic is that the terminology is not very consistent.
[47:43.100 --> 47:50.100]  You have to kind of just look up the latest buzzwords and latest sort of way people refer to it.
[47:50.100 --> 47:55.100]  In the book, the book talks about coarse-grained parallel versus massively parallel.
[47:55.100 --> 48:02.100]  So coarse-grained parallel machine is one where you have a small number of processors, but they are kind of powerful processors.
[48:02.100 --> 48:07.100]  Whereas in a massively parallel system, you have thousands and thousands of processors.
[48:07.100 --> 48:10.100]  And again, today we see a mix of these.
[48:10.100 --> 48:16.100]  And of course, again, this is not considering GPUs, which dominate the computing hardware today.
[48:18.100 --> 48:24.100]  In a parallel system, our optimization metrics change a little bit.
[48:24.100 --> 48:32.100]  In case of a single-site machine like we are running PostgreSQL on our laptop, we kind of care about how fast a query runs.
[48:32.100 --> 48:40.100]  But in a parallel system, we also care about the number of tasks that get done in parallel.
[48:40.100 --> 48:46.100]  These two metrics, throughput and response time, can conflict with each other.
[48:46.100 --> 48:53.100]  So you have to kind of choose, or the system has to choose, which way they want to optimize.
[48:53.100 --> 48:59.100]  Do you want to optimize the number of tasks you get done, or do you want to optimize the sort of latencies of tasks?
[48:59.100 --> 49:05.100]  And again, no easy answers here, because this is a more complex question.
[49:05.100 --> 49:08.100]  This one is a bit more straightforward. Speedup versus scaleup.
[49:08.100 --> 49:12.100]  Speedup is a fairly common word.
[49:12.100 --> 49:18.100]  Speedup, so let's say you have one machine, then I give you N machines.
[49:18.100 --> 49:24.100]  I give you N machines. How well are you able to use that?
[49:24.100 --> 49:26.100]  And that's the speedup.
[49:26.100 --> 49:34.100]  So the speedup is going to be the amount of time it took you to do the job on the smaller system,
[49:34.100 --> 49:38.100]  divided by the amount of time it took you to do the job on the larger system.
[49:38.100 --> 49:41.100]  Ideally, we would like this to be N.
[49:41.100 --> 49:44.100]  So we would like this to be like this.
[49:44.100 --> 49:49.100]  So as I give you more and more resources, your speed should go up.
[49:49.100 --> 49:50.100]  You should do it faster.
[49:50.100 --> 49:57.100]  If I give you 10 times more machines, you should do the job 10 times faster.
[49:57.100 --> 49:59.100]  So let's pause here, let's see.
[49:59.100 --> 50:01.100]  What do you think are the problems with that statement?
[50:01.100 --> 50:04.100]  Why wouldn't I be able to do that?
[50:04.100 --> 50:09.100]  So I have a query that's taking me currently 10 seconds on my laptop.
[50:09.100 --> 50:12.100]  And I say, OK, here are nine more laptops.
[50:12.100 --> 50:15.100]  Do it in one second.
[50:15.100 --> 50:22.100]  What would be some of the challenges in getting that done?
[50:22.100 --> 50:24.100]  Maybe it will improve performance at first.
[50:24.100 --> 50:29.100]  But after a certain extent, maybe the amount of processors won't improve.
[50:29.100 --> 50:34.100]  How quickly you can do the task, you have to change how you do the actual task.
[50:34.100 --> 50:41.100]  First of all, the first statement there is that at some point, adding more resources is not going to help at all.
[50:41.100 --> 50:42.100]  That is true.
[50:42.100 --> 50:45.100]  And secondly, you do have to change the way you do it.
[50:45.100 --> 50:47.100]  So first of all, it's not even clear.
[50:47.100 --> 50:51.100]  Like I have a PostgreSQL laptop like running on my laptop.
[50:51.100 --> 50:54.100]  I have a large database, let's say 10 gigabytes.
[50:54.100 --> 50:56.100]  It's taking me 10 seconds to do a query.
[50:56.100 --> 50:59.100]  I literally do not have a way to use the main laptops.
[50:59.100 --> 51:06.100]  OK, like PostgreSQL does not give me the ability to use those nine laptops, nine other machines.
[51:06.100 --> 51:10.100]  PostgreSQL is designed to be a single machine database system.
[51:10.100 --> 51:14.100]  It has no way to use those nine computers at all.
[51:14.100 --> 51:17.100]  Forget getting any performance out of it.
[51:17.100 --> 51:20.100]  There is just literally no way to do it.
[51:20.100 --> 51:24.100]  Because I can't tell PostgreSQL, hey, there are these other nine machines.
[51:24.100 --> 51:27.100]  It's like no mechanism to handle that.
[51:27.100 --> 51:30.100]  So first, I have to figure out how I'm going to change it.
[51:30.100 --> 51:38.100]  Secondly, even if I change it, there are other issues that will limit how much benefit I can get out of it.
[51:38.100 --> 51:48.100]  So more specifically, does anyone want to sort of suggest reasons why I cannot get linear speed up in a setting like this?
[51:53.100 --> 51:59.100]  So the communication between the laptops, the communication between the machines will cause delays.
[51:59.100 --> 52:04.100]  No matter what I do, even if I'm able to divide my work exactly among the 10 machines,
[52:04.100 --> 52:11.100]  which in itself is a sort of engineering challenge, the communication between them will cause delays.
[52:11.100 --> 52:14.100]  Another issue is the startup cost.
[52:14.100 --> 52:16.100]  There's a startup cost that's fixed.
[52:16.100 --> 52:21.100]  It takes some time to just initialize everything on all the machines.
[52:21.100 --> 52:23.100]  That cost cannot go away if I have 10 machines.
[52:23.100 --> 52:25.100]  I still have the same cost.
[52:25.100 --> 52:29.100]  So that also can cause problems.
[52:29.100 --> 52:32.100]  Anything else? Anyone want to suggest?
[52:35.100 --> 52:39.100]  There is a... Do I have the Amdahl's law? Okay, I don't have it here.
[52:39.100 --> 52:42.100]  Let's talk about this a bit more.
[52:42.100 --> 52:45.100]  There is a difference. There is a second metric you can measure.
[52:45.100 --> 52:55.100]  Instead of measuring small system elapsed time versus large system elapsed time, I can say, how much bigger task are you able to do?
[52:55.100 --> 53:02.100]  So if I give you n times more resources, how much bigger task can you solve in the same time?
[53:02.100 --> 53:06.100]  And that is often called a scale up.
[53:06.100 --> 53:15.100]  So you have an n times larger machine, n times more resources, but I also give you an n times bigger job.
[53:15.100 --> 53:20.100]  And so the scale up refers to how much bigger job can you do in the same amount of time?
[53:20.100 --> 53:29.100]  These two tend to be slightly different ones because this I could do with 10 laptops by running PostgreSQL on 10 machines separately.
[53:29.100 --> 53:33.100]  So instead of doing one job at a time, I could do 10 jobs in parallel.
[53:33.100 --> 53:39.100]  And so that way, the scale up of PostgreSQL might not be too bad.
[53:39.100 --> 53:47.100]  But speed up is a problem. So that's why these two are often mentioned separately.
[53:47.100 --> 53:53.100]  In the parallel computing literature, they are often called weak scaling versus strong scaling.
[53:53.100 --> 54:01.100]  If you have taken a parallel computing work, the similarity here is weak scaling versus strong scaling.
[54:01.100 --> 54:05.100]  Something like that. I am not super familiar with it.
[54:05.100 --> 54:09.100]  What are the factors that limit speed up and balance?
[54:09.100 --> 54:16.100]  So the first one is that there is some computation that you just cannot parallelize.
[54:16.100 --> 54:21.100]  And this is often discussed in the context of Amdahl's law.
[54:21.100 --> 54:27.100]  So let's assume that let's say p is the fraction of the task that you can parallelize.
[54:27.100 --> 54:34.100]  In that case, the remaining one, 1 minus p, is the task you cannot parallelize.
[54:34.100 --> 54:40.100]  So for instance, if your task was taking 10 seconds,
[54:40.100 --> 54:45.100]  and if one second is a part that you just cannot parallelize at all because it's a startup cost,
[54:45.100 --> 54:49.100]  you cannot go below one second. There is no way for it to go below one second.
[54:49.100 --> 54:51.100]  One second is a fixed cost you are paying.
[54:51.100 --> 55:00.100]  And so Amdahl's law codifies that by saying that if p is the fraction of the task that you can parallelize,
[55:00.100 --> 55:04.100]  then you cannot do better than 1 over 1 minus p plus p minus n.
[55:04.100 --> 55:12.100]  This is the fixed cost. This is the best speed up that you can get on the part that you can parallelize.
[55:12.100 --> 55:18.100]  So if p is 0.9, so 90% of the task can be parallelized, huge percent of the task can be parallelized,
[55:18.100 --> 55:22.100]  my best speed up is still going to be at most 10. I cannot go beyond that.
[55:22.100 --> 55:27.100]  Because there is 10% that I just cannot parallelize.
[55:27.100 --> 55:34.100]  Amdahl's law is also used to just say that when you are optimizing something, make sure it actually matters.
[55:34.100 --> 55:44.100]  Like you might have a great optimization for something that only constitutes 0.2 of the overall, 20% of the overall time.
[55:44.100 --> 55:51.100]  And if it only constitutes 20% of the overall time, you are not going to get much benefit out of optimizing it.
[55:51.100 --> 55:57.100]  Doesn't mean you shouldn't optimize it, but just be aware that your benefit out of optimizing it is going to be limited.
[55:57.100 --> 56:05.100]  Startup costs are a major problem for getting good speed up and scale up.
[56:05.100 --> 56:12.100]  Interference, which was mentioned earlier, as you are communicating across these machines,
[56:12.100 --> 56:19.100]  the network can become a bottleneck, the memory bus can become a bottleneck, they all kind of compete with each other.
[56:19.100 --> 56:24.100]  And then skew is a problem.
[56:24.100 --> 56:30.100]  So even if I am able to parallelize my task, I have a relation, a big relation, 10 gigabytes.
[56:30.100 --> 56:34.100]  And I say, okay, 10 machines, let me spread it out over the 10 machines.
[56:34.100 --> 56:40.100]  Well, the mechanism by which I spread it out is usually hashing or maybe range.
[56:40.100 --> 56:46.100]  But whichever way I spread it out, it's possible that one machine gets a large chunk of data and the other machines get small chunk of data.
[56:46.100 --> 56:53.100]  So that's skew, right? Like, for instance, if I were to look at the zip codes of everyone at campus,
[56:53.100 --> 56:57.100]  well, the college park zip codes are going to dominate that.
[56:57.100 --> 57:04.100]  That's the skew in the data and that can affect the degree of parallelism.
[57:04.100 --> 57:11.100]  What about distributed systems? Distributed systems are, so I make a distinction between parallel and distributed.
[57:11.100 --> 57:21.100]  The main difference is that in distributed, we have a wide area network involved in the middle.
[57:21.100 --> 57:35.100]  So a situation like this where there is a server here, a server in UK, a server in Argentina, I guess, another one down here, another one in China and so on.
[57:35.100 --> 57:38.100]  That's a distributed setup.
[57:38.100 --> 57:44.100]  In most cases, you do not do distributed for efficiency. You do it because you have no choice.
[57:44.100 --> 57:49.100]  You do it because you have users here, here, here, here.
[57:49.100 --> 57:53.100]  But you don't do it because you get more performance out of it. That doesn't make any sense.
[57:53.100 --> 57:58.100]  If you wanted more performance, I'll just put more machines right here.
[57:58.100 --> 58:02.100]  I would not distribute them across because that is a lot of work.
[58:02.100 --> 58:11.100]  Moving data around on a wide area network even today is a lot of work. It's expensive.
[58:11.100 --> 58:19.100]  So typically distributed systems are done for necessity, not for performance.
[58:19.100 --> 58:22.100]  So we will talk about both parallel and distributed at various points.
[58:22.100 --> 58:26.100]  But for query processing, we'll primarily focus on parallel.
[58:26.100 --> 58:36.100]  For transactions, we'll talk more about distributed because that's where the problem is more interesting and difficult.
[58:36.100 --> 58:44.100]  Let me pause here and see. Any questions on any of these materials so far?
[58:44.100 --> 58:49.100]  So what are some of the things we're going to talk about?
[58:49.100 --> 58:53.100]  From a data management perspective, we have a few questions here that we have to figure out.
[58:53.100 --> 58:58.100]  One is, given a whole bunch of machines, how am I going to partition my data across them?
[58:58.100 --> 59:05.100]  The word that often gets used here is sharding. So shard and partition are basically the same words.
[59:05.100 --> 59:10.100]  So we have the data. We have a relation. Let's say we want to spread it out.
[59:10.100 --> 59:16.100]  Because I have 10 machines or 100 machines, I would ideally spread out my data across them.
[59:16.100 --> 59:23.100]  So how do I do that? What is the mechanism for it? How do I use it?
[59:23.100 --> 59:28.100]  How do I execute an operation across a group of computers?
[59:28.100 --> 59:34.100]  So I have a join, I have a selection, I have an aggregate. How do I actually do that?
[59:34.100 --> 59:40.100]  Given that my data is spread out across a bunch of machines, I need to figure out how to execute my operations.
[59:40.100 --> 59:43.100]  How do I execute an update across them?
[59:43.100 --> 59:50.100]  So if I have an item that's present in multiple places, I kind of need to make sure that all the copies are updated.
[59:50.100 --> 59:54.100]  And this is a serious problem for distributed systems like Facebook.
[59:54.100 --> 01:00:00.100]  How to deal with failures? We haven't talked about failures too much, except for Apache Spark.
[01:00:00.100 --> 01:00:07.100]  But failures are just a normal thing in data centers. I need to be able to handle failures.
[01:00:07.100 --> 01:00:12.100]  In fact, this is one of the places where the NoSQL systems really benefited earlier.
[01:00:12.100 --> 01:00:15.100]  Because they could handle failures much better than parallel databases.
[01:00:15.100 --> 01:00:19.100]  Parallel databases did not do well with failures earlier. Now they do.
[01:00:19.100 --> 01:00:27.100]  But back then, 20 years ago, they did not handle failures well.
[01:00:27.100 --> 01:00:33.100]  So we'll talk about each one of these at some level, like some depth, different depths.
[01:00:34.100 --> 01:00:37.100]  But these are complicated topics.
[01:00:37.100 --> 01:00:45.100]  You would have to go significantly deeper in a graduate course to really understand all the details of them.
[01:00:48.100 --> 01:00:51.100]  So the first question, data partitioning.
[01:00:51.100 --> 01:00:59.100]  So I'm going to start making this more concrete now so that we can actually work out some of the techniques.
[01:00:59.100 --> 01:01:03.100]  Let's assume I have three sides. These are three machines.
[01:01:03.100 --> 01:01:06.100]  I have three machines. Each one of them has its own disk.
[01:01:06.100 --> 01:01:09.100]  So I'll discuss this all in a shared nothing context.
[01:01:09.100 --> 01:01:12.100]  Some of it applies to shared memory. Some of it applies to shared disk.
[01:01:12.100 --> 01:01:16.100]  We'll discuss it for shared nothing because that's the cleanest model we have.
[01:01:16.100 --> 01:01:21.100]  I have a relation of R. I have a relation R.
[01:01:21.100 --> 01:01:26.100]  The simplest way to spread it out across these three machines is through hashing.
[01:01:26.100 --> 01:01:28.100]  And that's very commonly done.
[01:01:28.100 --> 01:01:36.100]  So what I do is I have a hash function, and I hash tuples of R on one of the attributes, the primary key, basically.
[01:01:36.100 --> 01:01:39.100]  And I send some tuples here, some tuples here, some tuples here.
[01:01:39.100 --> 01:01:45.100]  R1, R2, R3 here refer to partitions of R, not different relations.
[01:01:45.100 --> 01:01:51.100]  So R1, R2, R3 are three partitions of R. S1, S2, S3 are three partitions of S.
[01:01:51.100 --> 01:01:57.100]  The standard semantics for us is that machine one can directly read the data on its own disk.
[01:01:57.100 --> 01:02:06.100]  But if it wants to read the data R2, let's say, it has to go through machine one because machine one does not have direct access to R2.
[01:02:06.100 --> 01:02:14.100]  So that's the basic setting that we will work out for the rest of this part.
[01:02:14.100 --> 01:02:20.100]  Any questions on this? Does this make sense what we're doing here?
[01:02:20.100 --> 01:02:24.100]  And this is typically how systems like Spark work, by the way.
[01:02:24.100 --> 01:02:32.100]  They do take a relation like this and spread it out across a large number of machines in this fashion.
[01:02:32.100 --> 01:02:37.100]  Sometimes using hashing, sometimes not. It really depends.
[01:02:37.100 --> 01:02:39.100]  It depends on how the relation came to be.
[01:02:39.100 --> 01:02:44.100]  Sometimes the relation, you get the relation in pieces.
[01:02:44.100 --> 01:02:49.100]  So you get, let's say, today's orders, tomorrow's orders, day after tomorrow's orders.
[01:02:49.100 --> 01:02:55.100]  They all become different partitions. Even though they are not like hashed or anything, they just become different partitions.
[01:02:55.100 --> 01:03:01.100]  So let's say something like Walmart, this might represent today's data.
[01:03:01.100 --> 01:03:08.100]  This might represent yesterday's data. This might represent the data from day before and so on.
[01:03:08.100 --> 01:03:18.100]  Any questions so far?
[01:03:18.100 --> 01:03:21.100]  So what are the advantages of doing this?
[01:03:21.100 --> 01:03:28.100]  By spreading out data like this, I can utilize the processing power of all the machines.
[01:03:28.100 --> 01:03:33.100]  So this can handle R1 data, this can handle R2, this can handle R3 and so on.
[01:03:33.100 --> 01:03:36.100]  For instance, let's say I wanted to do a count query.
[01:03:36.100 --> 01:03:39.100]  Well, machine one can go ahead and count all the tuples in R1.
[01:03:39.100 --> 01:03:42.100]  Machine two can count all the tuples in R2.
[01:03:42.100 --> 01:03:44.100]  Machine three can count all the tuples in R3.
[01:03:44.100 --> 01:03:48.100]  And then they just add them up across each other.
[01:03:48.100 --> 01:03:53.100]  So very simple to do, sort of, utilize the machines.
[01:03:53.100 --> 01:03:56.100]  And also, simple read-write queries.
[01:03:56.100 --> 01:04:04.100]  So if a user wants to read data from, let's say, today, there is a router here that simply sends the data here.
[01:04:04.100 --> 01:04:08.100]  Machine one reads it. Someone else wants to read yesterday's data.
[01:04:08.100 --> 01:04:10.100]  That query goes here and so on.
[01:04:10.100 --> 01:04:16.100]  That makes life easier because you can just sort of read-write queries can just be distributed across.
[01:04:16.100 --> 01:04:20.100]  The problem, of course, is that if I want to do a complex join or aggregate,
[01:04:20.100 --> 01:04:26.100]  then I have to get all the machines involved and combine all of the data.
[01:04:26.100 --> 01:04:33.100]  And that gets more expensive.
[01:04:33.100 --> 01:04:36.100]  Any questions so far?
[01:04:36.100 --> 01:04:41.100]  All right. So we'll discuss one more thing and then we'll stop for today.
[01:04:41.100 --> 01:04:48.100]  Another topic that we really need to sort of internalize here is replication.
[01:04:48.100 --> 01:04:56.100]  In most parallel distributed systems, data is replicated multiple times, at least two, usually more times.
[01:04:56.100 --> 01:05:03.100]  So this is an example where we are replicating all of it on three separate machines.
[01:05:03.100 --> 01:05:12.100]  The benefits of replication are that you can...
[01:05:12.100 --> 01:05:18.100]  The benefits of this type of replication is that if one machine fails, you don't lose any data.
[01:05:18.100 --> 01:05:26.100]  Whereas here, if this machine failed and this crashed or something, you lost R1.
[01:05:26.100 --> 01:05:32.100]  So that's a problem. In a parallel setting where failures are common, you can't just lose data like that.
[01:05:32.100 --> 01:05:35.100]  Even if you have a way to recover it, you can't just lose data like that.
[01:05:35.100 --> 01:05:46.100]  Whereas with replication, this machine goes away, we still have two more copies.
[01:05:46.100 --> 01:05:54.100]  The reads become very fast. If I have three copies, if a real query comes in, I can send it to any of the three machines.
[01:05:54.100 --> 01:05:59.100]  Now the downside is, if a write query comes in, I have to send it to all three machines.
[01:05:59.100 --> 01:06:05.100]  Because now a write update to R has to be reflected in all three bases.
[01:06:05.100 --> 01:06:10.100]  And that's a distributed transaction. It's not just a transaction, it's a distributed transaction.
[01:06:10.100 --> 01:06:13.100]  Distributed transactions are difficult.
[01:06:13.100 --> 01:06:21.100]  There have been probably like 100 or maybe 500 papers written on distributed transactions in the last 15 years.
[01:06:21.100 --> 01:06:23.100]  Actually, 500 is probably an undercount.
[01:06:23.100 --> 01:06:32.100]  We're probably talking more than 1000 research papers written on distributed transactions.
[01:06:32.100 --> 01:06:41.100]  So the disadvantage is that the updates are much worse and we have more complicated updates.
[01:06:41.100 --> 01:06:49.100]  Because you kind of need to update all copies simultaneously, in a sense.
[01:06:49.100 --> 01:06:56.100]  Now the setting that we need to worry about is a combination of sharding and replication.
[01:06:56.100 --> 01:07:02.100]  Today's modern data systems have sharding because they have lots of parallel machines.
[01:07:02.100 --> 01:07:08.100]  And then they have replication because they need failure, tolerance, and they need performance.
[01:07:08.100 --> 01:07:15.100]  So modern systems have both replication and sharding.
[01:07:15.100 --> 01:07:19.100]  In a single data center, you have replicas and sharding.
[01:07:19.100 --> 01:07:29.100]  Across sites, in a wide area setting, you have replication and sometimes sharding.
[01:07:29.100 --> 01:07:31.100]  But at least you have replication.
[01:07:31.100 --> 01:07:39.100]  Like Facebook data is replicated in many places across the world because they need that fast access.
[01:07:39.100 --> 01:07:42.100]  And so all of these issues, how do you execute queries?
[01:07:42.100 --> 01:07:45.100]  How do you handle transactions?
[01:07:45.100 --> 01:07:46.100]  How do you handle failures?
[01:07:46.100 --> 01:07:50.100]  All of these are highly amplified in these settings.
[01:07:50.100 --> 01:08:01.100]  That's partly why this area sees so much ongoing activity because these are just not easy problems to solve.
[01:08:01.100 --> 01:08:03.100]  And the amount of data we have keeps increasing.
[01:08:03.100 --> 01:08:07.100]  So we try to keep coming up with new techniques.
[01:08:07.100 --> 01:08:12.100]  That's why companies like Databricks, a company that started barely 13, 14 years ago,
[01:08:12.100 --> 01:08:19.100]  I don't even know what their current valuation is, but I believe it's in excess of $200-$300 billion right now.
[01:08:19.100 --> 01:08:23.100]  And Databricks started with Apache Spark.
[01:08:23.100 --> 01:08:25.100]  That was their initial product.
[01:08:25.100 --> 01:08:32.100]  And now it's this big company that's doing so many things.
[01:08:32.100 --> 01:08:33.100]  All right.
[01:08:33.100 --> 01:08:34.100]  So any questions on this?
[01:08:34.100 --> 01:08:37.100]  Otherwise, we'll stop here and we'll pick up from this point.
[01:08:37.100 --> 01:08:40.100]  One more slide here, but we can talk about that in the next class.
[01:08:40.100 --> 01:08:49.100]  So the plan for next class would be to finish the parallelization discussion and then move on to transactions if we have time then.
[01:08:49.100 --> 01:08:54.100]  And if no questions, I'll see you on Tuesday.
[01:09:02.100 --> 01:09:17.100]  Thank you.




 So, for the last topic of query processing and optimization, we are going to discuss how the operators are executed in a parallel setting. This turns out to be useful to understand because most modern platforms like Apache Spark and all the other ones that are similar to it, data warehouses, and even modern multi-core database systems, they all do parallelization of operators. They don't execute operators sort of the way we talked about it. PostgreSQL still does it that way. PostgreSQL, MySQL, they are old core bases. They haven't really changed to make a big difference, make a big change there, but they are also moving in that direction. Just to quickly review some of the things we talked about. So, we discussed the three types of architectures last time, shared memory, shared list versus shared nothing. As I mentioned, modern infrastructure tends to be like a big mix of them, which makes it really hard to build some of these techniques. Because the techniques that you build, let's say for a join algorithm, depend quite heavily on the actual architecture. And then, it's kind of a… I don't really like this style of research which takes a particular hardware and optimizes for that because you know, you can't build a general purpose system like that. You can't specialize to a hardware. But there is tons of research like that. But a common setup you might see is a multi-machine, multi-CPU machine with multiple CPUs or multiple cores. They each have their own memory, but they can read each other's memory and then the data is shared out. And then, this is one server. You may have thousands of such servers in a data center, hundreds of thousands of such servers. And so, now our goal is to figure out how we can utilize all of these compute resources. The alternative of making a single machine more and more powerful, which is often called scale-up, it doesn't really scale. You can't go beyond a point. There is limits to how much you can put into a single machine, whereas the shared nothing, where you just have hundreds of thousands of these machines, from a hardware perspective, it scales just fine. The challenge is figuring out how to use it. We talked about speed-up scale-up, we talked about Amdahl's law last time. As I mentioned last time, distributed systems are done more out of necessity. So, the distinction between distributed and parallel for me is that parallel is usually co-located. All the machines are in a single place, usually one data center. As a result of it, there is a very fast interconnect between them. So, the data transfers across the machines is fast. Whereas in a distributed setting, you have kind of transferring data over the wide area network, where bandwidth is an issue, and even worse, latency is an issue. You cannot get around the speed of light. So, there is going to be a delay going from here to here. There is no way around that. But you need to do distributed because your users are all over the place. So, you kind of have to do distributed. So, it is more out of necessity than necessarily choice. Any questions by the way on any of this? All right. So, the key questions that we are going to talk about today, or not all of them actually, we won't talk about updates right now, is how to partition the data across them and how to execute an operation across a group of computers. Okay. And we'll talk about it at a fairly shallow level, but it should be enough for you to get the ideas. The data partitioning is typically done through either some sort of a hash partitioning or range partitioning. So, what I mean here is that this is an example of three machines that are in a shared nothing setup. We have a relation R and we partition it into three parts, R1, R2, R3. These are called partitions. They are sometimes called shards. So, sharding and partitioning are treated equivalently in this context. Now, the question is how do I do this? The simplest way to do it is through hashing. So, you pick a key and you hash the key. So, you complete a hash value and that hash value sort of puts the tuple into one of these three machines. So, if you have an ID attribute, maybe you can use that for hashing. Just do ID like complete a hash function and then modulo 3 will give you the location of one of these three machines. So, you can also do range partitioning. So, do I have that as slides here or not? Sorry, hold on. Okay, I guess I thought I had some slides on it. Go ahead. So, for each R is a particular machine. So, each R partition, yes. So, there are two techniques here which looks like I don't have slides on it. The two standard techniques are hash based or range based. By range based I mean that all tuples between 0 and a million, ID equal to 0 and a million, go here, million and 2 million go here, more than 2 million go here. So, that's range partitioning. I'm taking an attribute and I'm dividing that attribute into ranges and using that to distribute it. So, that's a lot of things. But, Spark by and large relies on hash partitioning. They have their advantages and disadvantages. As usual, if I'm doing range partitioning, then range queries are easier to support on a single machine. If I'm doing range partitioning like I just mentioned, 0 to a million, million to 2 million, if I have a query that says get me all the documents between 100 and 200, well, I know they are on a single machine. All the documents with IDs 100 to 200 are on a single machine, but with hash partitioning, they are pretty much guaranteed to be all over. So, it increases the overhead in getting them together. But, there are pluses and minuses. Any questions? I thought there was a question there. Another thing that complicates our life in a distributed or parallel setting is replication. All modern big data systems like data lakes, data warehouses, they replicate data. They replicate data in large part because of, for two reasons actually. One is that failures is a major reason. If a data item is only on a single machine or a single disk, that's a serious problem for you. Because that disk could crash, like completely crash, and you could lose your data. So, three-way replication is standard in most modern systems, minimum three-way. So, what you see in modern systems is that, like assuming your data is actually important, it will be replicated three times inside a single data center, and it will be replicated at least another location geographically distance. So, the disaster recovery, if you have heard that term, disaster recovery requires you to have a copy of all of your data available in a distant location. So, that if the earthquake strikes here, your data there is still safe. If you do have replication, like as shown here, the same data item, same relation is replicated here, you cannot have the ability to run queries on all of those, any of those. So, you can naturally get sort of parallelism, because some queries can go to this machine, some queries can go to this machine and so on. So, that's the second benefit. And then, of course, if it's distributed replication, then you have the benefit of reducing the latencies by going to the data location that's closest to you. So, someone who is in Bombay, India, can go to the data center that's right there, versus having to come to the data center in Bay Area, let's say. The downside, of course, is that now, if I'm updating R, I have to update it in all three locations. So, my write cost goes up. My read cost is lower, but my write cost goes up. And then, the worst problem is that, it's not just that I have to update all three of them. I have to update them synchronously. I cannot have a possibility of, like it makes my life difficult if my updates get applied in different orders. For instance, I have two updates I'm making. They get applied to this copy in one order. They get applied to this copy in another order. That could be a serious problem. Like, imagine a situation where someone wants to post a sensitive document or a photo or something, but they want to restrict its visibility. Or restrict the visibility of their account. So, a standard thing you might do is, you might go and, let's say, make your account private. And then, you post the photo. Okay. Fine. Works out fine. On another replica, if these two operations get flipped, you might end up having the photo exposed before the privatization happens. So, the order in which the operations happen is important. They have to happen in the same order here and here. So, that's what I mean by this issue of concurrency control. You need to make sure that all of the updates happen and they happen in some required order or some required guarantees. Okay. So, that's a complicated problem. We'll talk about distributed transactions sort of towards the end of the semester. Many data management systems have all of these things. They have sharding. They have replication. They have replication within a data center. Replication outside a data center. And so, you know, this is partly why building these data management systems is so difficult. And it's a somewhat new problem. This problem has evolved over the last 15-20 years. We didn't have this problem 20 years ago. It was only when the cloud computing and data centers became prevalent that this problem became more and more important. Any questions by the way so far? Anyone good with this? We, the failures that we need to worry about. We have to worry about disk failures. Any disk can fail at any time. When you have a data center with potentially millions of disks, like hundreds of thousands at least, you are guaranteed disk failures every so often. Every few hours, every few minutes maybe. You might have network failures. Like the question is, let's say, like a simple question, right? I want to update R. Now my requirements are that all the copies are updated in synchron, like at the same time. But what if machine 3 is currently not reachable? Okay. So if machine 3 is not reachable, then what do I do with that update? Do I allow that update? Do I not allow that update? You know, doesn't have a simple answer. Okay. Has anyone here run into these problems anywhere in their internships or something? Or other work? Any experience with these things? Do you know how I might solve the like one simple solution to a problem like this might be? I have three replicas and I need to handle potential network failures. Okay. The simple approach will be that you will not allow R to be updated until all three machines are available. But there is an alternate option that is slightly better. Anyone? Want to guess? So a simple approach is to use quorums instead. You can say that as long as two out of three are available, I can make the update. Okay. And that in fact solves some of the problems. It's one solution to it. There are other solutions. But quorums are pretty common in these systems. Okay. But this is a rich literature and lot of different techniques here. So the guarantees that we typically want in this place are, again we are not going to talk about these right now today. But the guarantees we want typically are, we don't want to lose any data. Consistency, we want our transactions updates to be sort of consistent. And this is the one that parallel databases failed at before. If I have a complex analysis task that's running, I should be able to keep it running even if a machine fails. Okay. That's a hard one. So parallel databases couldn't handle that. I don't know what they do today. I don't know how many of them are able to handle that. But they can recover, they won't lose any data. But any queries that were currently running on that machine would have to be restarted. That's a serious problem. When I'm doing big machine learning training, let's say, or big video analysis of large number of videos, I can't restart it if a machine fails. I have to sort of continue from where I left off. And that's hard to do for parallel databases. So we're not really going to talk about these right now. This was just a setup for a conversation, like a discussion we'll have later. Our goal for today is somewhat more focused. We just want to understand how to parallelize some of the query processing operations. We won't really talk about query optimization per se. We'll primarily focus on the execution of the operations themselves. We'll start with a sort operation. Then we'll do a join and then we'll do group by. And that's pretty much it. The setup that we use is very similar to what we had on the previous slide. We have three machines. We'll use this as the example most of the time. There should be three. There are two relations, R and S. Both of them are three-way partition. So R1, R2, R3, S1, S2, S3. These are partitions of these relations, not copies. Just to be clear, the entire relation R is made up of R1 plus R2 plus R3. So the first question is sort. So let's say this is my data. I want to sort R. How should I do that? Suggestions on, maybe we can work this out together. I don't have, unfortunately, a paper here. But thoughts on how sorting of R might look like in a setup like this. It could be very basic to begin with. Anyone? And currently R is not sorted by whatever I want to sort it on. So it's kind of unsorted right now. Might be hashed or something, but I want to sort on zip code and it's not sorted on zip code. So in most of these cases, you can take the algorithms that you already know and kind of just apply them to this setting. Add extra steps to make them work for this setting. Anyone want to try, take a stab at doing this for sort? So sorting is relatively easy. What you do is each of the machines sorts their own relation, own partition. Okay. So I have a, yeah. So, and again, this requires coordination across the four machines. So remember, these are kind of the machines and we'll show the steps that happen on each of the machine. So I have four partitions of R. Each of the partition, each of the machines sorts its partition locally. Okay. So in this case, I sort R1 here. Okay. Sort R2 here. So I effectively get three sorted runs. Across them, they are not sorted because we made no assumptions about how R1, R2, R3, R4 are partitioned. So these are sorted now, but across them they are not sorted. Okay. So I kind of have to do a merge sort of sorts. Right. If all of them could fit on a single machine, then I can simply take this and kind of collect them onto a single machine. Okay. But in these settings, we are typically not allowed to do that because our data set is too large. So what we do instead is something like this. Okay. So this shows an example of sorting on a. So after R1, in fact I don't actually need to sort R1 here at all. What I do is I take all tuples from R1 with a less than 100 and I send it to this machine. Okay. Now note that it's the same machine in this case, but you can think of it as different machine. It doesn't really change things too much whether you think of it as the same machine or different machines. Okay. If it makes it simpler to think about it, you can imagine it is the same machine. So this here is not really transferring any data because it's just staying here. But this is transferring. Okay. So all the data between 100 and 200 is sent here. 200 and 500 is sent here. All the data more than 500 is sent here. So first machine there takes R1 and effectively splits it into four groups. Okay. And then they all do that. R2 also does that. R3 also does that. R4 also does that. As a result of it, all the tuples with a less than 100 are going to be on this machine now. And then this machine can sort those. All the tuples between 100 and 200 are here. 200 and 500 are here. More than 500 are here. Each of them sorts their own relation, own part. And now across them I have a total sorted order. Because of the way I partition them, this here is like put this next to this, this, this. That gives me the complete sorted order. Okay. So that's kind of a simple way to sort a big relation that cannot fit on a single machine. So I'm using all of my machines. As many as I have. I have 10,000 machines. I can use them if I have a really large enough data set. And there do exist data sets people try this on, which are very large. And I get my final sorted answer. Okay. So relatively simple, you know, if you sit down and try to figure out how to use multiple machines, this is the solution you're likely to come up with. Okay. Most of the ones we are going to discuss today are of that sort. They're not, they're relatively straightforward extensions of the single machine algorithms. Okay. Most of them involve partitioning of data. And all you have to do is figure out the right way to partition it. Any questions on this? I don't have a ton of slides for this part. I have like 10 slides so we can, maybe 15. So we can peacefully go through, like slowly go through this. Okay. Any questions on this? We're good with this? Now, this step here, the key thing to realize here is that here we are transferring almost all of R around. Okay. A large part of R1 will be sent from this machine to these three machines. A large part of R2 will be sent from that machine to these three machines and so on. This operation is often called a shuffle because we are taking our original data and effectively transferring it all over. Okay. This is a pretty expensive operation. Yeah. Because the data sets we are dealing with typically are very large and you know, the amount of data transfer required to do a sort can be quite significant. So systems like Spark or parallel databases, they spend a ton of time figuring out how to optimize some of these operations. So another question. What happens if the A8s to like A0s, what would happen? So since we're sending all of them into different machines depending on R2 like the relations, what would happen if there's a case where one machine has too much whether it can transfer over to the other ones or would it make another one? Yeah. And that's a very good question. So what if it turns out after I'm doing this that R.A less than 100 gets all the tuples. And so suddenly I end up with this machine having to deal with a large volume of tuples whereas these three machines get very little tuples. Okay. So this is usually called, this is often called skew. S-K-E-W. We didn't really talk about it so far too much but you know, it's an important problem for all of these systems. The solution is to make sure that these partitions that you chose are relatively good. Okay. So you, a priori before doing this operation, you try to make sure that your partitions are not going to have this problem. You can't guarantee it but you try to make sure it doesn't happen. So is the solution, is it trial and error solution? No, it's not trial and error. It's use the statistics. Remember all those statistics that we mentioned, histograms and things like that? You would use those for this purpose. So if I had a histogram on R.A, then I can use that to figure out the right range. With a histogram and if I don't have a histogram, I can do sampling. So these systems and these are all techniques that are implemented in modern systems. You would sample R and decide what the right partitioning is. Despite that, you could get it wrong but it's kind of unlikely. And nowadays, these systems have enough storage space to have large numbers of statistics. And there's enough time to do sampling. So it becomes a little less of an issue. It becomes a bigger issue for intermediate result. So it's okay if I'm sorting R. I have statistics on R. But if I do an R with a join S, then I have to sort the join result, then it becomes an issue. And at that point, things like sampling come into play. That is a good question. That skew is a pretty important problem for most of these systems. Any other questions? So here's an example of this actually working out if you did not follow that fully. So there is 12 tuples across these machines, 3 each. So for R1, A equal to 10 just goes to the same machine. If you assume it's the same machine, it just stays here. A equal to 100 goes to this machine. A equal to 300 goes to this machine and so on. And at the end of it, all of these machines have these tuples which they can now locally sort. And all of R is now sorted. It is still split up across these machines, but I know it's sorted. And so I can do things with it. Like for instance, if I want to do an aggregate operation, after the sort is done, I can do aggregate here, aggregate here and so on. I can do it separately. And sort of the big problems in parallel sort in terms of optimization are deciding how many sort of these. That is fixed in some ways because that's the original relation itself. But I could choose to distribute it across fewer or more machines depending on the size of the data set. So I can do it then skew is the second problem I have to worry about. Making sure that the ranges that I have used are reasonable. Because if I do get them wrong, I have to sort of figure out how to handle that. If somehow I get it wrong and too many tuples show up here, then that could be problematic. My speed up at that point goes down. So remember the speed up like I have four machines, so I expect the job to be done in let's say four times faster. But if it turns out that this machine gets a large chunk of the data, even if it is able to handle it, my speed up now goes down. Let's say this machine got half the data, the other machines got less data. Overall time for me is going to be dominated by this machine. And my maximum speed up is going to be less than two. So it's not just a question of there will be too much data on this machine, but also my overall latency goes up. Any questions? Okay. Let's talk about joins. Now we already talked about hash joins. Parallel joins look very much like hash joins. And since most joins tend to be equi joins, hashing works out really well for that. So here's an example of that. I have four machines as before. And I have two relations now, R and S. I'm trying to join them. What I do is I do hash partitioning of R and S. Okay. By partition by partition. So this is kind of what it looks like. I take R1 tuples from here and S1 tuples from here. Let's say my join is on attribute A and I have four values for the hash function. Everything with h of A equal to one goes to this machine. Everything with h of A equal to two goes to this machine and so on. And all of the initial machines do that. Okay. So this machine does the same thing. Same hash function. Everything with A equal to one goes here. A equal to two goes here and so on. So that's awesome. Okay. Now, because I'm, it's a equi join on A. So I'm looking for tuples with R dot A equal to S dot A. I'm guaranteed that each of the machines gets the tuples that join with each other. They don't all join with it because this is hashing. So multiple values of A will hash to the same hash value. But I will not have a situation where there is a R tuple here whose matching tuple is here. That cannot happen. Okay. Because if there is a R tuple here with A equal to 15, all the S tuples with A equal to 15 are also on this machine. Okay. There is no, not possible for a tuple with R dot A equal to 15 be here and S dot A equal to 15 be here. Just by construction, that's not possible. Okay. And so all I have to do at this point is to do a local join. I can use any technique I want. I can use hash technique. I can use sort technique. I can even use nested loops join. It doesn't really matter. So one thing that you see in this system is that the way you distribute the data and what you do inside each machine can be two separate things. They don't have to be the same thing. Okay. Again, I have a shuffle here. And in fact, here I'm shuffling both R and S. So I have a lot of data to be shuffled. But aside from that, this works out fine. So here is an example of it. See, I have R1 equal to 1R1, 3R2, S1 equal to 2S1, 3S2. So H of A here is identity, I think. Yeah, it's identity. So tuples just map to that same. H of A is equal to A. So if I look at this, this tuple, 1R1 ends up here. So do 1S4 and 1S3. Those also end up here. And same for 2, same for 3, same for 4. There are no S tuples with 4. So I get an empty set of tuples here. And then I do local joints here. I produce 1R1S4, 1R1S6. Here I produce 5 tuples or 4 tuples. Here I produce 3 tuples. Here I produce 0 tuples. And I'm done. Now in general, of course, this will actually be a proper hash function. So not all tuples will join at each other. But you are guaranteed to find the right join result this way. So this type of a join technique is very common in systems like Spark and parallel databases. Hash based join. Hash based join. Hash based join. Hash based join. Hash based join. Hash based join. Any questions on this? Is this fully clear? We'll see a couple of variations of this. I want to make sure this is clear before we look into the variation part. No questions? So here's a question for you. What if S is relatively small compared to R? Could we do better than what we just did here? So let's imagine that R is like 100 gigabytes. S in total, it's only 10 megabytes. It's still separated out in 4 partition but it's only 10 megabytes. So R is like 100 gigabytes. S is 100 megabytes. So huge difference. And so the question becomes could I do better? And these types of joins are not super uncommon. You're often doing lookups. Like joins are often lookups of some sort. And so it's not uncommon for the lookup table to be relatively small. But the table from which you are looking up, that table is relatively large. So that's not very uncommon. So the situation is that R is very large. So each of these partitions is like 25 gigabytes, let's say. Whereas each of the partitions of S is only 25 megabytes. So in that situation, could I do better? Anyone with thoughts? How can I reduce the amount of, the main question here is amount of data that's transferred. Network costs are what we worry about more in these settings than necessarily competition costs. So the, go ahead. I just had a question about this process. When they're being sent from the left to the right, they're not joined yet, right? They're joined after on the right once they're partitioned by like the attribute. Usually these things happen in steps. So you do, you do the partitioning and then in the second phase, you do the joins. Okay. So there's oftentimes a distinct, in parallel databases, you may often combine them. But in systems like Spark, there tends to be pretty distinct steps in there. Anyone with ideas on how to speed this up? If there is a huge asymmetry in the two relations? So the main thing that we want to try to avoid is because R is so large, we want to see if we can reduce the communication because of it. So the way that one works is something like this. So this is called a fragment and replicate technique. And this is asymmetric. So we assume here S is small. And it's relatively simple. What we do is, instead of partitioning R, we leave R where it is and we replicate S. We copy S all over. Okay. So the communication looks something like this. I send all of S1 to pulse to the first, to all the machines. Okay. The first machine, I show it as a communication here, but it's not actually communication because these two are often the same machine. But if, like, it's possible R1 and S1 are separate machines, in which case you might need to actually send it. And then, same happens with the other one. So this machine will read S3 and send it to all the machines. So the limit of it, every machine, it already has R1. This machine already has R1. It also has all of S. And this machine already has R2. Now it has all of S. And it can join each of those. Okay. So R1 joins with S on this machine. R2 joins with S here. R3 joins with S here. R4 joins with S here. So now, I'm spending more communication effort on S. Because now S is getting transferred more than it used to before. Because now, I'm effectively copying S3 on all the machines. So that's more that data transfer for S. But I'm not transferring the data for R at all. So if there's a significant asymmetry where R is much bigger than S, then this can be significantly faster than the previous approach. Okay. And again, if you think a little bit about it, this is a pretty natural way to take our standard hash join and adapt it to a parallel setting. But both of these options are implemented in systems like Spark or parallel databases. Because they make sense under different settings. Okay. Depending on the relative size of R and S and other factors, both of those approaches could make sense. Okay. And that's the job of the query optimizer to figure out which one of these to use. So query optimizer now has added responsibility over what we talked about so far. So far we talked about choosing the order of operations and so on. Now it also has to choose how to execute the specific operations in more detail. So, yeah. Any questions? Okay. As a last topic, let's talk about grouping aggregation, which is a very common operation, potentially more common than joins in parallel settings. So, we didn't really talk about MapReduce specifically, but Reduce operation that you might see being referred to in some programming languages, as well as in systems like Hadoop, which was quite popular about 15-ish years ago. So, the MapReduce is effectively the same as aggregates. Okay. So, for instance, grouping by let's say host names or words, this is all quite common operations. If you remember the Spark assignment that we did early on, you did a lot of reduce operations in there. Okay. And so this is a common operation and we need to figure out how to solve it in a parallel setting. These systems do make a difference between group by and aggregate. So, we will also talk about grouping and aggregate slightly separately. Okay. Some of the challenges we need to deal with are we may have a large number of keys. So, group by aggregates usually reduce the size of the data, but grouping doesn't reduce the size of the data. Okay. Aggregates reduce the size of the data. That's why Reduce and aggregate, which are very similar terms, they reduce the size of the data. Group by doesn't. Group by is simply repartitioning the data on the key. Okay. And so that's why you want to, that's why you need to optimize this. Under different scenarios, different options make more sense. Once again, we'll have the same setting that we did. Four machines with one partition of R per machine. And my goal is to do some sort of a group by aggregate. Let's say I want to do some. Okay. Group by some. So, how do I do that? We will work through a full example. So, here's an example. So, my tuples are of the form a, some number and rest. We don't care about the rest because we are going to do a sum on, we are going to do a group by on the first attribute, sum on the second attribute. Okay. So, my goal here is group by sum. In this case, the simplest technique for doing it is to first compute the partial group by's on each of the machines. Okay. So, on this machine, I compute a partial group by because I don't have all the tuples, that's why it's partial. I compute a and a nine. So, nine here because of five plus four nine. B, I have a single tuple B3, so that appears as this. So, it's a partial sum. Okay. But I do do a group by, so the amount of data might reduce. So, this result will likely be smaller than my original R1. Okay. And I do the same thing at every of, each of the machines. And, assuming that the number of keys is small. Okay. So, small number of groups. Assuming the number of groups is small, I can simply send all of these aggregates to a single machine. And that single machine can sort of do the full aggregation across them. Okay. So, this machine will get, oops. Okay. This machine will get A9 from this machine. It might get A10 from this machine, A15 from this machine, A30 from this machine, and it will simply add them up. Okay. So, as long as the number of groups, the number of keys, the separate values for the attribute are small, this approach works out fine. Okay. Any questions on this? No. So, again, the idea here is to perform the partial work on each of the machines, send all of that work to one machine, which finishes it. Okay. Remember, as we were talking about aggregates, we talked about how different aggregates require different techniques. So, I can do this for count, I can do this for sum, I can do it for average by sending two values, sum and count. And then this machine completes the actual average. But I can't do median this way. Okay. Because median mode, they require collecting all the values across all the machines together. So, in that case, I don't get a reduction in the relation. Okay. Not a significant reduction. Because I pretty much have to send the entire data from here to here. So, in those cases, this might not be acceptable. Because this requires me to combine all of them in a single machine. Okay. So, it might not be acceptable if I have a large number of groups. So, average would require sending sum and count. Amount of data transferred is typically low. And you do need a small number of groups for this approach to make sense. And it is often the case, you know, depending on what you are aggregating, you might have a small number of groups. What if I do have a large number of groups? Well, I do something very similar. I combine my previous slides approach. But I use hashing to redistribute the work. Okay. So, hashing can be seen as a way to redistribute the work here. So, here what I do is, I do the partial aggregates. But instead of sending them all to the same machine, I distribute them across four machines based on the value of the key. So, A go here, B go here, C go here and so on. Okay. And so, that way, and all of them do that. Okay. So, this one will send, the A partial aggregate here, B partial aggregate here, this one will send A partial aggregate here and so on. And that way, now, the amount of data I have on each of the machines is lower and I can compute the aggregates that I need separately here. So, at the end, my result will be distributed. The result for A will be here, the result for B will be here. But then, you know, depending on what I am doing next, that would be, you know, I will have to account for that in my next operation. So, again, the key idea here is we are doing the partial aggregation as I mentioned here and we use hashing to redistribute the work. The idea of using hashing to redistribute the work is very common in this setting. You see it pretty much in every system. Questions? All right, so, what if I don't have a reduce at all? Okay. If I don't have a reduce, then I am kind of doing something very similar to this, okay. But I am doing it without the reduce. So, there is no partial aggregates at that point. All I am doing is grouping. And so, in that case, let's say if I want to compute media or something like that, each of the machines will do grouping but it will not do any kind of reduction. And then it will redistribute the groups using hashing. So, everyone will send their A groups here, B group here, C group here. And then you will get the full groups here and then compute the aggregate or whatever you want to do here. Okay. So, this one and the previous one are very similar. Whether I have a large number of groups and aggregates or I don't have aggregates is kind of very similar approach. In both cases, we are doing at least somewhat as soon as possible to try to reduce the amount of data. And then I will redistribute using hashing so that I can form the groups appropriately on each of these machines. Okay. Any questions on these three techniques we saw? Okay. Again, the goal here was to give you a pretty introductory, like high level introduction to this topic. It's a big topic. If you work with big data systems, Spark would be the most common one but there are quite a few others. You will see things like this all over the place. Okay. Because this is the primary way systems like Spark execute database operations. Okay. If there are no more questions, just a few notes. If I have an operation like a selection operation, I can simply apply them to each of the machines separately. Okay. There are some optimizations you can do. So I won't go through the details of this optimization. But depending on exactly how you have partitioned the data, you might be able to speed it up a little bit. But worst case, you can just execute the selection operation in parallel across all the machines. Because all you are doing is choosing whether to pulse a stay or not and that can be done in parallel. Other relational operations like duplicate elimination, you can just use sorting for it. Some version of hashing can also be used for this purpose. The operation like project can just be done in parallel. So most of these are natural generalizations of what we saw before. If you understand these three, four techniques here that we talked about, redistributing work across machine, how do you get the tuples at the same place using hashing, that kind of handles the key sort of foundations of all parallel operators. Any questions on any of these things that we talked about so far? So if there are no questions, we are kind of done with what I wanted to cover with the query of processing and optimization. This is a huge topic. I mean we just did the intro of this topic in my graduate course. I go in depth on just this topic. Query processing and optimization is what we discussed through the length of the semester. And I don't even talk about transactions anymore. I used to cover both query processing and transactions at one time. But now we have 624 which covers transactions in more detail. And in 724 we focus more on the query processing and parallelization aspects and so on. But we started with discussing the high level query execution, operators in a query, how to connect them, pipeline versus materialization. Different types of operations, different types of operations, sorting or hashing based. Query optimization we talked about briefly. And then the parallel and distributed execution. The next topic for us is transactions which will kind of be the rest of the semester. So before we, since we have some time, we will get started on that. Are there any questions on any of this? And again, for quiz, next week's quiz will kind of stop here. So, because transactions will be covered kind of really little bit. So it doesn't make a lot of sense to include that in next week's quiz. So next week's quiz will be on effectively these 3-4 parts. Some of the joints, external memory, query optimization and parallel distributed execution. So hash joints and nested loops were in the previous one. So it's more like this. And also aggregates. I don't have aggregates listed here, but aggregates are also in there. So the first question is, perhaps not off? Homework? Sure it's not off? Right. Maybe I put a wrong date on it. But it is finished. Like I have put the homework together and maybe I missed the date. I missed the date on that. Yeah. It was out briefly and at that time I wasn't finished with it. So I sort of changed the date and I guess when changing it, I must have changed it by too much. Yeah. Yeah, it shows as being available at 2 PM today. So I won't change it right now since it's only 40 minutes from now. Okay. Any other questions? There's also the postgres assignment that's out. I hope at least one of you has started working on it because they reached out to me. But I hope more of you are doing it. And we'll discuss exactly how that will be graded and so on very soon.
